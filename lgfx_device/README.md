<!--
SPDX-FileCopyrightText: 2026 Masatoshi Nishiguchi

SPDX-License-Identifier: Apache-2.0
-->

# lgfx_device

`lgfx_device/` は、LovyanGFX に面するネイティブ適合層です。

担当範囲:

- 描画対象の解決
- 装置境界での検証と意味
- スプライト、パレット、画像、文字の装置側動作
- 版を固定した LovyanGFX 副リポジトリとの薄い統合

AtomVM の項は解析せず、NIF 応答も組み立てません。

ネイティブ操作契約は [操作契約](../docs/protocol.md)、リポジトリ全体の構成は [構成](../docs/architecture.md) を参照してください。

## ファイル構成

- `lgfx_device.h`
  - NIF 層が使う装置向け公開適合面
- `lgfx_device_internal.hpp`
  - 内部宣言と共有する適合処理
- `state.cpp`
  - 装置状態、初期化、パネルとタッチの設定、対象解決、短冊表示状態、遅延確保
- `control.cpp`
  - 初期化、終了、回転、明るさ、色深度などの表示制御
- `text.cpp`
  - 文字操作、文字種プリセット、カーソル、折り返し、LovyanGFX 風文字倍率
- `primitives.cpp`
  - 基本図形の描画
- `images.cpp`
  - JPEG と RGB565 画像操作
- `sprites.cpp`
  - スプライトとパレットのライフサイクル、基準点、転送、回転・拡大縮小
- `clip.cpp`
  - クリップ矩形
- `fonts/generated/`
  - 有効時に部品へ組み込む生成済み文字データ

## 責務の分離

NIF 層で重複させるべきでない装置意味を、この層が担当します。

例:

- 対象が存在するか
- スプライト番号がすでに確保されているか
- スプライトがパレット付きか
- 転送先スプライトが存在するか
- `pushImage` の行幅とデータ長が妥当か
- 回転・拡大縮小の意味が妥当か
- LovyanGFX 風数値引数を最終検証し、固定版 LovyanGFX の呼び出し形へ渡すこと

## 設計方針

この層は意図的に小さく保ちます。

- NIF や操作契約の関心を適合層へ持ち込まない
- AtomVM と項の処理を適合層へ持ち込まない
- LCD の物理制御と論理描画を分ける
- 必要に応じて、論理描画を描画対象の境界へ通す
- 互換用の足場を最小限にする

LovyanGFX 全体を写し取ることが目的ではありません。このリポジトリが公開する API に合う、小さく明示的な適合面を提供し、メモリー所有権を予測しやすく保つことが目的です。

## 現在のモデル

ネイティブ側は、実行時設定と稼働中装置の所有権を分離します。

- 実行時設定は NIF の初期化時に明示的なスナップショットとして渡す
- 稼働中の LCD 装置は単一実体として扱う
- この層は、不透明な所有者識別子を使い、所有者を考慮した初期化、終了、寸法照会を解決する

描画対象は直接解決します。

- 対象 `0` は稼働中の LCD 装置
- 対象 `1..254` は明示的に作成したスプライト

したがって、この層は単一実体の所有権と対象の意味を扱いますが、NIF の項や応答生成は扱いません。

## この層を変更する場合

- NIF の項変換と操作検証は `lgfx_port/` と `../docs/protocol.md` に置く
- この層は対象解決、所有権、装置意味へ集中させる
- 外部から見える契約が変わる場合だけ、操作契約文書を更新する
